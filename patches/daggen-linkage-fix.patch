diff --git a/.gitignore b/.gitignore
index 0331bbb..9dd42dd 100644
--- a/.gitignore
+++ b/.gitignore
@@ -12,6 +12,7 @@
 *.dylib

 # Executables
+daggen
 *.exe
 *.out
 *.app
diff --git a/daggen.c b/daggen.c
index 0fdcc31..75062dc 100644
--- a/daggen.c
+++ b/daggen.c
@@ -34,13 +34,17 @@ int main(int argc, char **argv)
   DAG dag;
   int i;

-  srand((unsigned int)getpid() + (unsigned int)time(NULL));
   /* parse command line options */
   if (parseOptions(argc, argv) == -1) {
     printUsage();
     exit(1);
   }

+  if (global.has_seed) {
+    srand(global.seed);
+  } else {
+    srand((unsigned int)getpid() + (unsigned int)time(NULL));
+  }

   /* putting header */
   fprintf(OUTPUT, "// DAG automatically generated by daggen at %s// ",
diff --git a/daggen_commons.c b/daggen_commons.c
index 0a005b7..2135df6 100644
--- a/daggen_commons.c
+++ b/daggen_commons.c
@@ -16,6 +16,8 @@

 #include "daggen_commons.h"

+Global global;
+
 /*********************************/
 /** Command line option parsing **/
 /*********************************/
@@ -43,8 +45,7 @@ int parseOptions(int argc, char *const *argv) {
   int maxalpha_flag = 0;
   int regular_flag = 0;
   int jump_flag = 0;
-  int dot_flag = 0;
-
+  int seed_flag = 0;
   global.output_file=stdout;
   global.jump=1;
   global.mindata=2048;
@@ -57,6 +58,8 @@ int parseOptions(int argc, char *const *argv) {
   global.regular=0.9;
   global.n=100;
   global.dot_output=0;
+  global.has_seed=0;
+  global.seed=0;

   while (ret_val == 0) {

@@ -70,6 +73,7 @@ int parseOptions(int argc, char *const *argv) {
         { "maxalpha", 1, 0, 0 }, /* double   */
         { "regular", 1, 0, 0 }, /* double   */
         { "jump", 1, 0, 0 }, /* int   */
+        { "seed", 1, 0, 0 }, /* unsigned int */
         { "dot", 0, 0, 0},
         { 0, 0, 0, 0 } };

@@ -201,10 +205,11 @@ int parseOptions(int argc, char *const *argv) {
         global.jump=jump;

       } else if (!strcmp(optname, "dot")) {
-
-        dot_flag = 1;
-
         global.dot_output=1;
+      } else if (!strcmp(optname, "seed")) {
+        seed_flag = 1;
+        global.has_seed=1;
+        global.seed=(unsigned int)strtoul(optarg, NULL, 10);
       } else {
         fprintf(stderr, "unknown switch: %s\n", optname);
       }
@@ -287,6 +292,9 @@ int parseOptions(int argc, char *const *argv) {
       fprintf(stderr,"Warning: using default regular value (%g)\n",
           global.regular);
     }
+    if (!seed_flag) {
+      fprintf(stderr,"Warning: using time/process-derived random seed\n");
+    }

   }

@@ -318,6 +326,7 @@ void printUsage(void)
       "\t --ccr <communication(MBytes) to computation(sec) ratio>\n"
       "\t --jump <number of levels spanned by communications>\n"
       "\t    jump=1: perfectly synchronized levels\n"
+      "\t --seed <random seed>\n"
       "\t --dot: output generated DAG in the DOT format\n"
       "\n");
   return;
diff --git a/daggen_commons.h b/daggen_commons.h
index a0a259a..95ea1e9 100644
--- a/daggen_commons.h
+++ b/daggen_commons.h
@@ -41,9 +41,11 @@ typedef struct {
   double minalpha, maxalpha; /* Amdahl's law parameter */
   int jump;
   int dot_output;
+  int has_seed;
+  unsigned int seed;
   FILE *output_file;
 } Global;
-Global global;
+extern Global global;

 typedef struct _Task *Task;
 typedef struct _DAG *DAG;
